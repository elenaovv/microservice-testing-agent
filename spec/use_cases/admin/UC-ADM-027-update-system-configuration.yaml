id: UC-ADM-027
role: admin
name: Update System Configuration
preconditions:
  authenticated_as: ROLE_ADMIN
  state:
    - at least one configuration entry exists
goal: The admin modifies an existing system configuration.
success_criteria:
  - the configuration reflects the modified value