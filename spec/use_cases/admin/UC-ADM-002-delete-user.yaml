id: UC-ADM-002
role: admin
name: Delete User
preconditions:
  authenticated_as: ROLE_ADMIN
  state:
    - at least one non-admin user account exists
goal: The admin removes an existing user account.
success_criteria:
  - the account no longer appears in the user list