id: UC-ADM-036
role: admin
name: View All Orders
preconditions:
  authenticated_as: ROLE_ADMIN
  state: []
goal: The admin requests every order in the system across all users from the admin order panel.
success_criteria:
  - the system returns every order regardless of owner