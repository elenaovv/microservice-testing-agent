id: UC-ADM-030
actor: admin
name: Delete Contact (Admin Panel)
preconditions:
  authenticated_as: ROLE_ADMIN
  state:
    - at least one contact exists
goal: >
  The admin removes an existing contact from the admin contact panel.
success_criteria:
  - the contact no longer exists
  - the contact no longer appears in the admin contact list
smith_equivalent: AdminContactList.java
notes: ""
