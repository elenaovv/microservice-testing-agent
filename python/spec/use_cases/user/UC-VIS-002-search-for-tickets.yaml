id: UC-VIS-002
role: user
name: Search for Tickets
preconditions:
  authenticated_as: none
  state: []
goal: A visitor specifies an origin station, destination station, and date to find available train trips using the basic search.
success_criteria:
  - the system returns a list of trips matching the query
  - each returned trip exposes train, departure time, and seat availability