# Capture

Helpers for saving prompt and run-input artifacts.

- `prompt_capture.py` writes the prompt inputs and generated prompt text used for a run.

Use this package for reproducibility artifacts. It should not build prompts, generate tests, or summarize results.
